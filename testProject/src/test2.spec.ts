
import * as ts from "typescript";
import { expect } from "chai";

describe("Test Project second test file", () => {
    it("test passes", () => {
       expect(true).to.equal(true);
    });
});